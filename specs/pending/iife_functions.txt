notes: """
  When functions are immediately-invoked, they can be converted into a `do`
  construct.  The caveat here is that the function arguments must be either empty
  or identical (eg, `((a,b) -> )(a,b)`).
"""
----
(function () {
  hello();
})();
----
do ->
  hello()
